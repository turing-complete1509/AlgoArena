FROM node:20-alpine

# Install Docker client so we can run Docker commands (DooD)
RUN apk add --update docker openrc

WORKDIR /app/backend

COPY backend/package*.json ./
RUN npm ci --production

COPY backend/ ./

ENV NODE_ENV=production
CMD ["npm", "run", "worker"]
